# Определяет локальные переменные (идентификаторы образов, пути к директориям), упрощающие работу с кодом и уменьшающие дублирование.

locals {
  ubuntu_image_id = "fd8vhban0amqsqutsjk7"
  logbroker_path  = "/home/ubuntu/logbroker"
  # ... другие локальные переменные
}
