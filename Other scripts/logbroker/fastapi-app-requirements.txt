# Файл содержит список зависимостей Python‑проекта с указанием минимальных версий пакетов: FastAPI (веб‑фреймворк), uvicorn (ASGI‑сервер) и httpx (HTTP‑клиент).

fastapi>=0.100.0
uvicorn[standard]>=0.22.0
httpx>=0.24.0
